package kotoba;

/**
 * KotobaLoader provides static methods to load file contents and check file existence.
 */
class KotobaLoader
{
	/**
	 * Retrieves the content of the file at the specified path.
	 * @param path The path to the file.
	 * @return The content of the file as a String, or null if the file does not exist or cannot be read.
	 */
	public static dynamic function getContent(path:String):Null<String>
	{
		return null;
	}

	/**
	 * Checks whether a file exists at the specified path.
	 * @param path The path to the file.
	 * @return True if the file exists, false otherwise.
	 */
	public static dynamic function exists(path:String):Bool
	{
		return false;
	}
}
